<%= render "topics/header" %>
<div class="card mx-auto">
  <div class="card-body">
    <h4 class="card-title"><%= @topic.title %></h4>
    <h6 class="card-subtitle mb-2 text-muted">by <%= @topic.user.name %></h6>
    <p class="card-text"><%= @topic.text %></p>
    <% if current_user == @topic.user%>
      <%= link_to "編集する", edit_topic_path(@topic), class: "badge badge-warning" %>
      <%= link_to "削除する", root_path, method: :delete, class: "badge badge-warning" %>
    <% end %>
  </div>
</div>
<%# <div class="topic__image">
  <%= image_tag @topic.image %>
</div>
<br>