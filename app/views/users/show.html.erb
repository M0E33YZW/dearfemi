<div class="card">
  <div class="card-body">
    <h4 class="card-title"><%= @name %>さんの投稿一覧</h4>
    <% @topics.each do |topic| %>
      <p class="card-text">
        <h6><%= link_to topic.title, topic_path(topic)%></h6>
        <%= topic.created_at %>
      </p>
    <% end %>
  </div>
</div>
